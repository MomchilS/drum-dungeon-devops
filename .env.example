# Drum Dungeon — environment template
# Copy to .env and set real values. Never commit .env.
#   cp .env.example .env

# Data directory for practice data (JSON backup)
# Local: path on your machine. Docker Compose: use /app/practice_data (set in compose).
PRACTICE_DATA_DIR=/srv/practice-data

# Environment: local, staging, production
ENV=local

# Database URL (local dev: localhost; Docker Compose: set by compose from DB_ROOT_PASSWORD)
# DATABASE_URL=mysql+pymysql://root:YOUR_PASSWORD@localhost/drum_dungeon

# Required for Docker Compose and for create_db.py (local)
DB_ROOT_PASSWORD=your_secure_root_password

# Required. Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET_KEY=generate-a-secure-random-key-minimum-32-bytes
